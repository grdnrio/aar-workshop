---
driver:
  name: vagrant

provisioner:
  name: chef_zero

verifier:
 name: inspec

platforms:
  - name: ubuntu-14.04
    customize:
      cpus: 2
      memory: 1024

suites:
  - name: default
    run_list:
      - recipe[aar-workshop::default]
    attributes:
